@model IEnumerable<FamilyPoints.Domain.Behavior>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Points)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Points)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BehaviorID }) |
            @Html.ActionLink("Details", "Details", new { id=item.BehaviorID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BehaviorID })
        </td>
    </tr>
}

</table>
